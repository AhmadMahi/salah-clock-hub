// Copy this file to secrets.h and fill in your own WiFi details.
// secrets.h is git ignored, so your password never reaches the repo.
#pragma once

#define WIFI_SSID     "YOUR_WIFI_NAME"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
